<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>disable</Nullable>
    <LangVersion>latest</LangVersion>
    <AssemblyName>crabeltest</AssemblyName>
    <RootNamespace>OceansCrabel.Tests</RootNamespace>
  </PropertyGroup>

  <!-- Math is pure and ATAS-free, so it can be exercised without the platform. -->
  <ItemGroup>
    <Compile Include="../CrabelMath.cs" />
  </ItemGroup>

</Project>
